{ 
  "name": "inforequestv1",
  "metadata": { "type":"info", "version": "1" },
  "allowedgroups": [ "administrationgroup", "teachergroup" ],
  "form": {
    "title": "My new info panel",
    "rows": [
      { 
        "row":"1",
        "fields": [
          { "type":"textarea", "validation":"max_len,2500", "name":"name", "label":"Name", "placeholder":"Name", "value":"name", "width":"6" },
          { "type":"currency", "validation":"required|numeric", "name":"amount", "label":"Amount", "placeholder":"10.0", "value":"amount", "width":"6" }
        ]
      },
      { 
        "row":"2",
        "fields": [
          { "type":"date", "validation":"max_len,10", "name":"duedate", "label":"Due date", "value":"duedate", "width":"12" },
          { "type":"hidden", "validation":"required|numeric", "name":"id", "value":"id" }
        ]
      }
    ]
  },
  "request": {
    "parameters": [
      { "type":"int", "validation":"required|numeric", "name":"id" }
    ]
  },
  "query": {
    "type":"select",
        "fields": [
          "id",
          "name",
          "amount",
          "duedate"
        ],
      "entity":"requestv1",
    "conditions":[
      { "field": "id", "operator": "=", "value": "id" }
    ]
  }
}
